<%= form_with(model: purr, local: true) do |form| %>
  <% if purr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purr.errors.count, "error") %> prohibited this purr from being saved:</h2>

      <ul>
      <% purr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
